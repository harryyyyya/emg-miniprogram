# Duo S competition v1

This directory is a new package rooted at `/root/duo_fc_competition_v1`. It does not overwrite D3 or `/root/duo_fc_f8`.

Current status is `PACKAGE_PREPARED_AWAITING_DUO_ACCEPTANCE`. The inherited Stage H/I evidence is retained, but it is not represented as evidence generated by this package.

Evidence boundary:

- Prompt 10 recorded BF16/F2 replay: inherited PASS.
- Prompt 13 HCI plus BLE adapter offline validation: inherited PASS.
- BT-11 live GATT: `NOT_RUN`.
- J3 to STM32: `NOT_RUN`; the packaged default is an exact-byte file sink.
- Real backend: `NOT_RUN`; the packaged default is a local mock.
- Actuator: `NOT_RUN`.

On Duo S, run:

```sh
cd /root/duo_fc_competition_v1
DUO_UART_SINK="file:$PWD/logs/uart_acceptance.bin" DUO_BACKEND_MODE=mock sh bin/preflight.sh
sh bin/run_board_acceptance.sh
```

Only after `logs/acceptance_*/acceptance_summary.json` says PASS may the package be reviewed for:

```text
READY_FOR_3H_PHYSICAL_INTEGRATION_WITH_EXPLICIT_LIVE_GATES
```

That status still does not mean BT-11, STM32, the real backend, or the actuator passed.

